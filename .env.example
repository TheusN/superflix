# ====================================
# SUPERFLIX - Variáveis de Ambiente
# ====================================
# Copie este arquivo para .env.local e preencha os valores
# cp .env.example .env.local

# -------------------------------------
# TMDB API (OBRIGATÓRIO)
# -------------------------------------
# Crie uma conta em: https://www.themoviedb.org/signup
# Pegue sua chave em: https://www.themoviedb.org/settings/api
NEXT_PUBLIC_TMDB_API_KEY="sua_chave_tmdb_aqui"

# -------------------------------------
# SUPABASE (OBRIGATÓRIO para produção)
# -------------------------------------
# Crie um projeto em: https://supabase.com
# Obtenha as credenciais em: Settings > API
# Sem essas variáveis, o sistema usa memória (dados perdem ao reiniciar)

# URL pública do Supabase (usada no cliente)
NEXT_PUBLIC_SUPABASE_URL="https://seu-projeto.supabase.co"

# Chave pública do Supabase (usada no cliente)
NEXT_PUBLIC_SUPABASE_ANON_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# URL do Supabase (usada no servidor)
SUPABASE_URL="https://seu-projeto.supabase.co"

# Chave de serviço do Supabase (NUNCA exponha no cliente!)
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."

# -------------------------------------
# JWT / AUTENTICAÇÃO (OBRIGATÓRIO em produção)
# -------------------------------------
# Use o mesmo JWT Secret do Supabase (Settings > API > JWT Settings)
# Ou gere um novo: openssl rand -base64 32
JWT_SECRET="seu_jwt_secret_aqui"

# -------------------------------------
# POSTGRESQL DIRETO (OPCIONAL)
# -------------------------------------
# Usado apenas para scripts de setup (npm run db:setup)
# Obtenha em: Settings > Database > Connection string (URI)
# POSTGRES_URL="postgres://postgres.[ref]:[password]@aws-0-us-east-1.pooler.supabase.com:6543/postgres"

# -------------------------------------
# CONFIGURAÇÕES OPCIONAIS
# -------------------------------------
# URL base da aplicação (para links e callbacks)
# NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Desabilitar proxy (se tiver problemas de conexão)
# NEXT_PUBLIC_USE_PROXY="false"
